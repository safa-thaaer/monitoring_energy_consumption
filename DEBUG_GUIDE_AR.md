# 🔍 كيف تفحص البيانات الواصلة من Firebase

## المشكلة
القدرة (الواط) أو أي قيمة ما توصل للتطبيق من Firebase

---

## ✅ الخطوات للفحص:

### **1. شغل التطبيق مع Debug Console**

```bash
flutter run
```

### **2. راقب Debug Console (سجل الأحداث)**

بعد ما يشتغل التطبيق، راقب الرسائل في الـ Console. ستشوف رسائل مثل:

```
📦 البيانات الكاملة المستلمة: {power_W: 0, energy_kWh: 0, voltage_V: 0, current_A: 0}
⚡ power_W = 0 (نوع: int)
🔋 energy_kWh = 0 (نوع: int)
⚡ voltage_V = 0 (نوع: int)
🔌 current_A = 0 (نوع: int)
🌍 co2_kg = null (نوع: Null)
✅ القيم النهائية - Power: 0.0 W, Energy: 0.0 kWh, Voltage: 0.0 V, Current: 0.0 A
```

---

## 🔎 تحليل الرسائل:

### **حالة 1: جميع القيم = 0**
```
⚡ power_W = 0 (نوع: int)
```

**المشكلة:** ESP32 يرسل أصفار فعلاً

**الحل:**
1. افحص توصيل المستشعرات (Current Sensor, Voltage Sensor)
2. افحص كود ESP32 - هل يقرأ القيم صح؟
3. افتح Serial Monitor في Arduino IDE وشوف القيم قبل الإرسال
4. استخدم زر الاختبار 🧪 لإضافة بيانات تجريبية

---

### **حالة 2: الحقل = null**
```
⚡ power_W = null (نوع: Null)
```

**المشكلة:** الحقل مو موجود في Firebase

**الأسباب المحتملة:**
1. **اسم الحقل مختلف**: ربما ESP32 يرسل `power` بدلاً من `power_W`
2. **المسار غلط**: البيانات مو في `/readings/timestamp/`
3. **البيانات ما وصلت**: ESP32 مو متصل بالإنترنت

**الحل:**
افتح Firebase Console → Realtime Database وشوف:
- هل البيانات موجودة؟
- شنو اسم الحقول بالضبط؟

**مثال إذا الاسم مختلف في Firebase:**

إذا لقيت في Firebase:
```json
{
  "readings": {
    "1738185000000": {
      "power": 1234.5,  // بدون "_W"
      "energy": 1.234,   // بدون "_kWh"
    }
  }
}
```

لازم تعدل الكود:
```dart
power = (data['power'] ?? 0.0).toDouble();  // بدلاً من 'power_W'
energy = (data['energy'] ?? 0.0).toDouble(); // بدلاً من 'energy_kWh'
```

---

### **حالة 3: النوع مختلف**
```
⚡ power_W = 1234 (نوع: int)  // المفروض double
```

**المشكلة:** ESP32 يرسل int بدلاً من double

**الحل:** الكود يحول تلقائياً بـ `.toDouble()` - ما في مشكلة

---

### **حالة 4: القيم String**
```
⚡ power_W = "1234.5" (نوع: String)
```

**المشكلة:** القيم مرسلة كـ نص

**الحل:** عدل الكود لتحويل String إلى double:
```dart
power = double.tryParse(data['power_W']?.toString() ?? '0') ?? 0.0;
```

---

## 📋 جدول الأخطاء الشائعة:

| الرسالة في Log | المعنى | الحل |
|---------------|--------|------|
| `power_W = 0` | يرسل صفر | افحص المستشعرات / استخدم بيانات تجريبية |
| `power_W = null` | الحقل مو موجود | تحقق من اسم الحقل في Firebase |
| `البيانات الكاملة = null` | ما في بيانات أصلاً | تحقق من المسار `/readings` |
| `error: permission denied` | قواعد Firebase ترفض القراءة | عدل Firebase Rules |

---

## 🧪 اختبار سريع:

### **اختبر بدون ESP32:**
1. اضغط زر الاختبار 🧪 في التطبيق
2. راقب الـ Logs
3. يجب أن تشوف:
```
⚡ power_W = 1543.2 (نوع: double)
✅ القيم النهائية - Power: 1543.2 W
```

إذا ما اشتغل، المشكلة في التطبيق نفسه.
إذا اشتغل، المشكلة من ESP32.

---

## 🔧 فحص Firebase مباشرة:

### **1. افتح Firebase Console:**
https://console.firebase.google.com/

### **2. اذهب إلى Realtime Database**

### **3. شوف البيانات:**

**بيانات صحيحة:**
```json
{
  "readings": {
    "1738185000000": {
      "power_W": 1234.5,
      "voltage_V": 220.0,
      "current_A": 5.6,
      "energy_kWh": 1.234,
      "co2_kg": 0.494
    }
  }
}
```

**بيانات غلط (أصفار):**
```json
{
  "readings": {
    "1738185000000": {
      "power_W": 0,
      "voltage_V": 0,
      "current_A": 0,
      "energy_kWh": 0
    }
  }
}
```

**بيانات غلط (أسماء مختلفة):**
```json
{
  "sensor_data": {  // ❌ المسار غلط - المفروض "readings"
    "power": 1234.5  // ❌ الاسم غلط - المفروض "power_W"
  }
}
```

---

## ✅ الحل النهائي:

بعد فحص الـ Logs:

### **إذا المشكلة من ESP32:**
- أصلح توصيل المستشعرات
- عدل كود ESP32
- استخدم بيانات تجريبية للاختبار

### **إذا المشكلة من أسماء الحقول:**
- عدل الكود ليقرأ الأسماء الصحيحة
- أو عدل كود ESP32 ليرسل الأسماء الصحيحة

### **إذا المشكلة من Firebase Rules:**
```json
{
  "rules": {
    "readings": {
      ".read": true,
      ".write": true
    }
  }
}
```

---

**الآن شغل التطبيق وراقب الـ Logs عشان تعرف المشكلة بالضبط! 🔍**
